package io.github.shogowada.statictags.generator.attribute

import java.nio.file.Path

class AttributeCodeGenerator {

  def generate(baseDirectory: Path, attributeSpecs: Iterable[AttributeSpec]): Boolean = {
    false
  }
}
