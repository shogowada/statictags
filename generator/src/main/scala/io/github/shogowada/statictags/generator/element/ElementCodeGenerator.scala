package io.github.shogowada.statictags.generator.element

import java.nio.file.Path

class ElementCodeGenerator {

  def generate(baseDirectory: Path, elementSpecs: Iterable[ElementSpec]): Boolean = {
    false
  }
}
